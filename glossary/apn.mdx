---
title: "APN"
description: "An Access Point Name (APN) defines the network path between a mobile network and an IP network (the public internet or a private network, for example, Eseye's MPLS network). Each Eseye APN consists of:"
---

<p>An Access Point Name (APN) defines the network path between a mobile network and an IP network (the public internet or a private network, for example, Eseye's <a href="/glossary/glossary">MPLS</a> network). Each Eseye APN consists of:</p>

<ul> <li>Pre-existing configurations between Eseye and the operator to enable a connection between the networks</li> <li>A RADIUS server and billing function</li> <li>The DNS – For more information, see <a href="/connectivity/anynet-dns-service">Understanding the AnyNet Domain Name Service (DNS)</a>.</li> </ul>
